(()=>{
const $=id=>document.getElementById(id),n=v=>Number.isFinite(Number(v))?Number(v):null,fmt=(v,d=1)=>n(v)==null?'—':n(v).toFixed(d);
function adRead(b){const r=n(b.ad_ratio),a=n(b.advancers),d=n(b.decliners);if(r==null)return['Building','Advance/decline data is still building.'];if(r<.55)return['Selling is broad',`${a??'—'} advancers versus ${d??'—'} decliners. Sellers have control across the tape, not just in a few large stocks.`];if(r<.85)return['Breadth is weak',`${a??'—'} advancers versus ${d??'—'} decliners. Participation is tilted negative, but not at a full capitulation extreme.`];if(r>1.8)return['Buyers are broadening out',`${a??'—'} advancers versus ${d??'—'} decliners. Buying is broad enough to matter as potential reversal confirmation.`];if(r>1.15)return['Buyers have the edge',`${a??'—'} advancers versus ${d??'—'} decliners. Participation is improving and supports stabilization.`];return['Balanced','Advancers and decliners are relatively balanced, so breadth is not giving a strong turning-point signal.']}
function rsiRead(b){const x=n(b.rsi_below_30),prev=n(b.rsi_below_30_prev);if(x==null)return['Building','RSI breadth is still building across the S&P 500 universe.'];const falling=prev!=null&&x<prev-2;if(x>=30)return['Broadly washed out',`${fmt(x)}% of S&P 500 stocks have RSI below 30. That is broad exhaustion territory; it raises rebound potential but does not prove a low is in.${falling?' The share is already shrinking, an early stabilization clue.':''}`];if(x>=20)return['Oversold breadth',`${fmt(x)}% of stocks are individually oversold. Selling is broad enough to watch for exhaustion.${falling?' Fewer stocks are now oversold than the prior session, which is constructive.':''}`];if(x>=10)return['Stress is spreading',`${fmt(x)}% of stocks have RSI below 30. Weakness is broadening, but it is not yet a market-wide washout.`];return['Not broadly oversold',`Only ${fmt(x)}% of S&P 500 stocks have RSI below 30. Index weakness is not yet producing broad momentum exhaustion.`]}
function trinRead(b){const x=n(b.trin),prev=n(b.trin_prev);if(x==null)return['Building','Volume breadth is still building.'];if(x>=2)return['Capitulation-like volume',`TRIN is ${fmt(x,2)}. Declining stocks are attracting disproportionately heavy volume — the kind of pressure that can appear near panic lows. It is an exhaustion clue, not a buy signal.${prev!=null&&x<prev?' It has eased from the prior session, which is worth watching.':''}`];if(x>=1.3)return['Selling pressure is heavy',`TRIN is ${fmt(x,2)}. Down-volume is running heavier than the issue count alone would suggest. That confirms stress, but not yet a reversal.`];if(x<=.7)return['Buying pressure is strong',`TRIN is ${fmt(x,2)}. Advancing stocks are carrying disproportionately strong volume — useful confirmation if it follows a washout.`];return['Volume pressure is balanced',`TRIN is ${fmt(x,2)}. Volume is not showing an extreme imbalance between advancing and declining stocks.`]}
function pcRead(pc){const raw=n(pc.value),p=n(pc.percentile_60d);if(raw==null)return['Unavailable','Equity put/call data is unavailable.'];if(p==null)return['History building',`Today’s equity put/call ratio is ${fmt(raw,2)}. The percentile needs more recent daily observations before it can say whether that reading is truly unusual.`];if(p>=90)return['Fear is unusually high',`Today’s ratio is ${fmt(raw,2)}, which sits in the ${Math.round(p)}th percentile of the recent 60-session distribution. The raw ratio tells you today’s positioning; the percentile tells you how extreme that positioning is versus the current market environment.`];if(p>=75)return['Hedging is elevated',`Today’s ratio is ${fmt(raw,2)} and ranks in the ${Math.round(p)}th percentile of the last 60 sessions. Traders are buying more protection than usual, but this is not automatically capitulation.`];if(p<=20)return['Positioning is complacent',`Today’s ratio is ${fmt(raw,2)}, only the ${Math.round(p)}th percentile of recent readings. The market is using relatively little put protection versus its own recent history.`];return['Positioning is ordinary',`Today’s ratio is ${fmt(raw,2)}, around the ${Math.round(p)}th percentile of recent readings. Options positioning is not at a meaningful sentiment extreme.`]}
function synthesis(d){const b=d.breadth||{},pc=d.equity_put_call||{},ad=n(b.ad_ratio),rsi=n(b.rsi_below_30),rprev=n(b.rsi_below_30_prev),tr=n(b.trin),tp=n(b.trin_prev),pp=n(pc.percentile_60d);const stress=[ad!=null&&ad<.7,rsi!=null&&rsi>=20,tr!=null&&tr>=1.3,pp!=null&&pp>=75].filter(Boolean).length;const improving=Number(ad!=null&&ad>1.1)+Number(rsi!=null&&rprev!=null&&rsi<rprev-2)+Number(tr!=null&&tp!=null&&tr<tp&&tr<1.2);if(stress>=3)return['Capitulation watch','Several internals are flashing stress at the same time. That raises the odds that selling is becoming exhaustive, but the turn is not confirmed until breadth improves and volume pressure starts to normalize.'];if(stress>=2)return['Stress is building','More than one internal measure is deteriorating. The market is getting closer to a tradable exhaustion setup, but buyers have not yet supplied enough confirmation.'];if(improving>=2)return['Stabilization is emerging','Breadth and/or volume pressure are improving after weakness. That is the transition Market Pulse wants to see before treating an oversold condition as a potential short-term turn.'];return['No major turning-point signal','Internals are not showing a coordinated washout or reversal confirmation. The primary market regime should continue to carry more weight than short-term oscillators.']}
function card(label,value,status,copy,foot){return `<article class="tp-card"><div class="tp-card-top"><span>${label}</span><strong>${value}</strong></div><h3>${status}</h3><p>${copy}</p>${foot?`<small>${foot}</small>`:''}</article>`}
function ensure(){if($('turningPointInternals'))return;const watch=$('guideWatch'),breadth=document.querySelector('.breadth-module');const html=`<section id="turningPointInternals" class="guide-section turning-points"><div class="guide-heading"><div><div class="section-step"><b>5</b> TURNING POINT INTERNALS</div><h2>Is selling exhausting — and are buyers starting to return?</h2></div><p>These are short-term confirmation tools. They can identify stress, capitulation and stabilization, but they do not override the primary trend by themselves.</p></div><article class="tp-summary"><div class="insight-label">TURNING POINT READ</div><h3 id="tpSummaryTitle">Building the internal read…</h3><p id="tpSummaryText"></p><div class="tp-flow"><span>Participation</span><b>→</b><span>Oversold breadth</span><b>→</b><span>Volume pressure</span><b>→</b><span>Sentiment extreme</span></div></article><div id="tpGrid" class="tp-grid"></div><div class="tp-guide"><strong>How to use this section</strong><p>A washout becomes more interesting when several internals become extreme together. A potential low becomes more credible when A/D improves, RSI breadth stops worsening, TRIN falls back toward or below 1, and elevated put/call positioning begins to normalize.</p></div></section>`;(watch||breadth)?.insertAdjacentHTML(watch?'afterend':'beforebegin',html)}
function renumber(){const path=$('readingPath');if(path)path.innerHTML='<span class="path-item"><b>1</b>Regime</span><span class="path-arrow">→</span><span class="path-item"><b>2</b>Market Health</span><span class="path-arrow">→</span><span class="path-item"><b>3</b>What Changed</span><span class="path-arrow">→</span><span class="path-item"><b>4</b>Watch Next</span><span class="path-arrow">→</span><span class="path-item"><b>5</b>Turning Points</span><span class="path-arrow">→</span><span class="path-item"><b>6</b>Breadth</span><span class="path-arrow">→</span><span class="path-item"><b>7</b>Stress</span><span class="path-arrow">→</span><span class="path-item"><b>8</b>Leadership</span><span class="path-arrow">→</span><span class="path-item"><b>9</b>Index Detail</span>';const set=(sel,v)=>{const b=document.querySelector(sel+' .section-step b');if(b)b.textContent=v};set('.breadth-module','6');set('.shared-context','7');set('#guideLeadership','8');set('.asset-switcher-wrap','9')}
function render(d){ensure();renumber();const b=d.breadth||{},pc=d.equity_put_call||{},[st,sc]=synthesis(d);$('tpSummaryTitle').textContent=st;$('tpSummaryText').textContent=sc;const [as,ac]=adRead(b),[rs,rc]=rsiRead(b),[ts,tc]=trinRead(b),[ps,pcopy]=pcRead(pc);$('tpGrid').innerHTML=[card('A/D BREADTH',n(b.ad_ratio)==null?'—':`${fmt(b.advancers,0)} / ${fmt(b.decliners,0)}`,as,ac,'Category: Participation · Advancers versus decliners'),card('RSI BREADTH',n(b.rsi_below_30)==null?'—':`${fmt(b.rsi_below_30)}%`,rs,rc,'Category: Breadth exhaustion · % of S&P 500 stocks with RSI < 30'),card('S&P 500 TRIN',n(b.trin)==null?'—':fmt(b.trin,2),ts,tc,'Category: Volume pressure · Arms-style formula on the S&P 500 breadth universe'),card('PUT/CALL PERCENTILE',n(pc.percentile_60d)==null?'—':`${Math.round(pc.percentile_60d)}th`,ps,pcopy,`Category: Sentiment extreme · Raw ratio today: ${n(pc.value)==null?'—':fmt(pc.value,2)}`)].join('')}
async function boot(){try{const r=await fetch(`data/market_context.json?v=${Date.now()}`,{cache:'no-store'});if(!r.ok)return;render(await r.json())}catch(e){console.warn('Turning points:',e)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',()=>setTimeout(boot,300));else setTimeout(boot,300);document.getElementById('refresh')?.addEventListener('click',()=>setTimeout(boot,1000));
})();
