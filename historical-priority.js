(()=>{
async function prioritize(){try{const select=document.getElementById('forwardStudySelect');if(!select)return;const r=await fetch(`data/event_studies.json?v=${Date.now()}`,{cache:'no-store'});if(!r.ok)return;const d=await r.json();const studies=d.studies||[],events=d.current_events||[];const byId=new Map(studies.map(s=>[s.study_id,s]));const tactical=events.find(e=>byId.get(e.study_id)?.category==='One-week participation');const preferred=tactical||events[0];if(!preferred||select.value===preferred.study_id)return;const option=[...select.options].find(o=>o.value===preferred.study_id);if(!option)return;select.value=preferred.study_id;select.dispatchEvent(new Event('change',{bubbles:true}));let note=document.getElementById('autoStudyNote');if(!note){note=document.createElement('div');note.id='autoStudyNote';note.className='auto-study-note';select.closest('.forward-selector')?.appendChild(note)}note.textContent=tactical?'Showing today’s most relevant tactical signal automatically.':'Showing today’s highest-priority triggered setup automatically.'}catch(e){console.warn('Historical study priority:',e)}}
function boot(){[900,1800,3200].forEach(ms=>setTimeout(prioritize,ms))}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',boot);else boot();document.getElementById('refresh')?.addEventListener('click',()=>setTimeout(boot,1500));
})();
